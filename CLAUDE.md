# 一步一江湖 — 給 Claude 的專案須知

純靜態網頁遊戲:用真實運動累積六維武學,用走路步數推進江湖劇情。無建置步驟,
以 `fetch` 載入 `data/`,所以要用本機伺服器開,不能雙擊 `index.html`。

**使用者是這個專案的設計者本人**,不是委託方。回報一律用白話,不要規格式敘述
(他明講過看不懂);平衡數值由他拍板,我的工作是**把實跑模擬的數字攤開**再讓他選。
回報只講成品長什麼樣子,不要出現檔名、欄位名、測試項數。

## 分工(2026-09-15 使用者定案)

**這個 repo 還有 OpenAI Codex 在做**,它讀的是 [AGENTS.md](AGENTS.md),不讀這份。

- **我(Claude Code)**:劇本、過稿,以及把事件寫進 `data/events.json` 到能跑為止
  (察覺段、出名後的版本、再次遇到的版本、地域與前置條件);事件資料的稽核測試
- **Codex**:畫面、程式、引擎(`src/`)、美術、互動與動畫
- **使用者**:只判斷最終成品是否符合他要的樣子

劇本需要引擎還沒有的能力時,寫需求給 Codex,**不自己改 `src/`**。

⚠️ 兩個 AI 共用同一個工作目錄。動手前先 `git status`:**不是自己做的未提交改動,
不 commit、不 reset / checkout / stash**,留著並在回報裡講。commit 只 add 自己的檔案。

---

## 兩個口令

### 「繼續工作」

這台電腦要接續**另一台**的進度。依序做:

1. `git pull`(遠端是兩台之間唯一的橋)
2. 讀 [交接.md](交接.md) —— 上一台的收工紀錄:做完什麼、下一步、等他拍板的事
3. 用白話回報「上次做到哪、我接下來要做什麼」,然後**直接開始做**,不要再問一次要不要開始

⚠️ **不要試圖從對話歷史找上下文** —— 換一台電腦就是全新對話,
上一台的討論不會跟著走。`交接.md` 才是唯一的真相來源。

### 「工作結束」/「收工」

這台今天做完了,下次換另一台。依序做:

1. `git status` 掃一遍 → **自己的**改動全部 commit + push;Codex 還沒提交的改動不碰,在回報裡講出來
2. **更新 [交接.md](交接.md)**:今天做完什麼、下一步、等他拍板的事
   (寫在對話裡沒用,另一台看不到)
3. 確認 `git status -sb` 顯示與 origin 同步;剩下的未提交檔若都是 Codex 的,照實回報
4. 回報一句「另一台 `git pull` 就接得上」+ 待決清單

---

## 鐵律

- **數值一律進 `data/*.json`,程式碼不寫死。** 調平衡不該動到 `src/`。
- **改過 `data/` 就要遞增 `src/ui/app.js` 的 `DATA_VERSION`**,否則 GitHub Pages
  會餵舊的快取資料檔給玩家。(這個常數在 Codex 的程式檔裡,已請它搬進 `data/`。
  搬好之前,若 `app.js` 有 Codex 未提交的改動,先回報,不要連它的改動一起提交。)
- **決議要回寫** [一步一江湖_設計總綱_v2.md](一步一江湖_設計總綱_v2.md)(開工唯一依據)
  與 README 的里程碑單。設計者會憑體感推翻先前定案,**發現新指示與總綱舊文互斥時
  要主動標出哪一條作廢**,不要沉默沿用。
- **事件文案改動走過稿流程**:提案 → 設計者拍板 → 入庫。生產規範見
  [事件庫生產規格書.md](事件庫生產規格書.md);過稿進度見各批次的過稿台帳。
- 每次改完跑 `npm test`;動到平衡數值再跑兩支模擬腳本。

## 常用指令

```bash
npm test                        # 單元測試
npm run serve                   # http://localhost:5173
node scripts/simulate.mjs 1000  # 蒙地卡羅平衡模擬
node scripts/stresstest.mjs     # 疊加壓測(exit 0 = 全過)
```
