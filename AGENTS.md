# 一步一江湖 — 給 Codex 的專案須知

這個 repo 有兩個 AI 在做:**Claude Code** 跟你(**Codex**)。
你們看不到對方的對話,只看得到 repo,所以分工和不能踩的線寫在這裡。
完整的專案規則在 [CLAUDE.md](CLAUDE.md),請一併讀。

## 分工(2026-09-15 使用者定案)

| 誰 | 負責 |
|---|---|
| **Claude Code** | 劇本與過稿;把事件寫進 `data/events.json` 到能跑為止(察覺段、出名後的版本、再次遇到的版本、地域與前置條件);檢查事件資料有沒有死路的稽核測試 |
| **Codex** | 畫面、程式、引擎(`src/`)、美術素材、互動與動畫 |
| **使用者** | 只判斷最終成品是否符合他要的樣子,不審技術細節 |

> 這一版取代 `docs/ui-refresh.md` 開頭那句「Codex 負責已通過劇本的整合」。
> 原因:劇本跟它的設定是綁在一起的。察覺段、出名版本、只在哪裡遇得到,
> 任何一項漏掉,內容就會悄悄失效,拆給兩個人,中間最容易掉東西。

劇本需要引擎還沒有的能力(新的條件、新的效果)時,Claude Code 會寫需求給你,不會自己改 `src/`。

## 同一個工作目錄,輪流做

- 動手前先 `git status`。看到不是你做的未提交改動,**不要 commit、不要 reset / checkout / stash 掉它**,
  留著,在回報裡講出來。
- commit 只 add 自己改的檔案,不要 `git add -A`。
- **推上 `master` 就是發布**:GitHub Pages 直接吃 master。還沒給使用者看過的東西不要推。

## 一件請你處理的交界

`DATA_VERSION`(讓 GitHub Pages 不要拿舊的快取資料)現在寫死在 `src/ui/app.js` 裡。
結果是 Claude Code 每改一次劇本資料,都得去改你的程式檔,兩邊會撞在同一個檔案上。
**請把它搬到 `data/` 底下的一個資料檔,由程式讀取。** 搬完之後改劇本就不必碰程式,
並請同步改掉 `CLAUDE.md` 裡「改過 data/ 要遞增 src/ui/app.js 的 DATA_VERSION」那句。

## 事件密度改 2000(2026-09-15 設計者定案,等你的引擎改動才能生效)

設計者定案:**每 2000 步觸發一件事**(現在 1000)。理由與模擬數字見 `決策頁_事件密度.md`。
數字在 `data/events.json` 的 `config.stepsPerEvent`,但**不能只改數字**,有兩個引擎問題:

1. **老存檔會卡死。** `pendingEventCount = floor(steps.total / 密度) − steps.resolved`,
   而 `resolved` 是「已觸發件數」。密度從 1000 變 2000,已走 20 萬步、觸發 200 件的存檔
   待觸發數變成 −100,要再走 202,000 步才會遇到下一件。
2. **`startNextEvent` 的檢查沒帶 data。** `src/engine/game.js` 裡 `if (pendingEventCount(state) <= 0)`
   永遠用預設 1000,跟畫面(有帶 data)算的不一致。

需要的行為:

- 密度改變時,老存檔**已經走了、還沒滿一件的零頭保留**,待觸發數不會是負的,
  換算後再走滿一個新密度的步數就正好多一件。
- **`steps.resolved` 不能往回改。** 它同時是事件冷卻(`events2.js` 的 `resolved − last.n`)
  和耳語冷卻的時鐘,倒退會讓事件長時間卡在冷卻。建議另記一個基準(例如換算當下的已用步數
  與當下的 resolved),待觸發數用相對於基準的差來算。
- 冷卻仍以「件數」計,**不必**跟著密度換算——密度減半、冷卻實際天數自然拉長,
  決策頁的模擬就是這樣算的。
- 測試至少涵蓋:上面那個 20 萬步/200 件的存檔換到 2000 後待觸發 ≥ 0;再走 2000 步剛好多一件;
  零頭保留;冷卻時鐘不倒退;新存檔從零開始每 2000 步一件。

做完後,**同一次 commit 裡**把 `stepsPerEvent` 改成 2000(`note_stepsPerEvent` 一併改成「已生效」),
並把 `index.html` 的預設字、`README.md`、`一步一江湖_設計總綱_v2.md` 裡「每 1000 步」的地方改成 2000,
遞增 `DATA_VERSION`。這幾個檔你手上都有未提交的改動,所以由你一起改,不會跟 Claude Code 撞檔。

## 事件條件要能「某件事發生過就不再出現」(2026-09-15,收線批次需要)

劇本開始收線:玩家答應過的事辦完之後,原本那件事件不該再出現。
例:臨江渡那個抱著食盒等兒子的婦人——兒子回家以後,她不會再坐在渡口等。

現在 `conditions` 只有 `requireFlags`(全部要有,每組可用 `a|b` 表示擇一),**沒有「有這個旗標就不出現」**。
用 `weightFlags` 給負數也不行:抽選的總權重會被扣小,抽選邏輯會算錯。

需要的行為:

- 新條件 `conditions.forbidFlags: ["x", "y|z"]`:清單裡**任何一項**成立(該組任一旗標為真)就不進抽選池。
  寫法跟 `requireFlags` 對稱,方便劇本一眼看懂。
- 規格書的條件欄位說明,Claude Code 會在收線批次一併補上。
- Claude Code 會先把 `forbidFlags` 寫進資料(引擎還沒支援前會被忽略,不影響現況),
  並加一項 `todo` 測試說明預期行為;你實作完把 `todo` 拿掉即可。
