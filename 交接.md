# 交接

> 兩台電腦靠這個 repo 接力。**對話歷史不會跟著走,只有這個檔案會。**
> 每次收工更新這一份;到另一台先 `git pull`,再讀這裡。

## 換一台電腦接手,照這兩步做

**① 開 PowerShell,把專案更新到最新**

```
cd $HOME\Desktop\wuxia-fitness; git pull
```

**② 在那台開 Claude Code,第一句話就說:**

> 先讀 交接.md,我要接著做

之後照這個檔的「下一步」往下走就行。**不必跟 Claude 解釋上一台做了什麼**——
那是這個檔的工作,對話歷史不會跟著走,但這個檔會。

**收工時**:跟 Claude 說「工作結束」,它會把東西推上去並更新這個檔。

### 手機也接得上

`CLAUDE.md` 跟著 repo 走,所以 <https://claude.ai/code> 在手機瀏覽器開起來,
第一句一樣打「繼續工作」就接得上(repo 是公開的,連得到)。
適合批稿、拍板、想設計;**看 diff 和審測試輸出在手機上很痛苦,那些留給電腦。**

> ⚠️ 加上手機之後,可能改同一個 repo 的地方變成三個。規矩不變:
> **一邊收工推上去,另一邊才接手。**

### 卡住的時候

| 狀況 | 怎麼辦 |
|---|---|
| `git pull` 說有衝突 | 多半是上一台忘了推。把整段錯誤貼給 Claude,它處理 |
| 那台還沒抓過專案 | 照 [docs/新電腦安裝指南.txt](docs/新電腦安裝指南.txt) 走一次 |
| 想先玩玩看 | `npm run serve`,然後開 http://localhost:5173 |

> ⚠️ 兩台不要同時改。一台收工推上去,另一台才 pull 接手。

---

**上次收工:2026-08-23(晚)**|**在哪台:D 槽那台**|**當時 HEAD:`18368c3`**

---

## 一、現在的狀態

Phase 2 進行中。測試 **301 項**全過,**事件庫 96 件**,**輿圖上線且江南已開放**,工作目錄乾淨。
線上版:<https://b0936261476-collab.github.io/wuxia-fitness/>

### 這次(2026-08-23)做了什麼

**輿圖第一批上線(§9.10)** —— 設計者五題全部拍板後實作:
`data/map.json`(六州 28 地點 31 條路)、`src/engine/map.js`、輿圖分頁、抵達敘事、
區域事件加權、兩層門檻(輿圖道具 + 歷練)。**步數就是趕路**:同一批步數同時推路程與事件,
路程不自己記帳而是拿 `steps.total` 的差額算,天生不會重複扣也不會漏算。

**B10 江南入庫(86 → 96)** —— 十件全過稿(其中兩件依批語修正)。
第一件〈鏢旗換圖〉是**鑰匙**:輿圖第一批只做了門檻,沒有任何事件會發圖,
江南本來會永遠去不了。三條分支**都給圖**(含接鏢失敗那條),已寫成測試。

**順帶補的三個引擎缺口**:
- `effects.mapGrant` —— 事件可以把一州的輿圖交給玩家
- `conditions.atProvince` / `atLocation` —— **江南的事只在江南遇得到**。
  沒有這個,輿圖只是換了張皮的分頁。實測:中原小鎮抽不到 JN 事件,揚州抽得到四件,
  鑰匙只在裴家鏢局抽得到
- 江南 `detour` —— 「只問路」不必判定也不必付出,沒有代價就完勝另外兩條選項。
  代價做成**路變遠**(第一趟多走 20,000 步),走過一次就不再罰

**兩件過稿修正**(設計者批語):
- 〈橋上讓路〉察覺分支「一直說看得見看不見很饒口」→ 整條重寫成具體動作
  (你拱手的方向不是朝她,是朝劍)
- 〈雨打芭蕉〉「如果我是有名的人,我怎麼知道不認得我的時候是給湯」→
  **這是邏輯漏洞不是文筆問題**:有名的玩家沒經歷過那個版本,那句比較是作者在比。
  名人版整段重寫成角色感覺得到的東西(門關得比剛才快)

## 二、下一步(建議順序)

> 設計者的話:「**江南才剛開,建議先讓它跑一陣子。**」所以下面不急著開工,
> 等他玩過幾天有體感再說。

1. **兩份舊的過稿文檔還沒收到回覆**(不擋任何事,但別忘了):
   `事件庫_批次B9_試玩版.md`(遭遇系統 6 件)、`江湖快報_試玩版.md`
2. **輿圖第二批** —— 各州專屬事件 + NPC 出沒權重(揚州易遇蘇挽秋之類)。
   江南跑順了再說;要開北疆的話**記得先寫一件發北疆輿圖的事件**
   (測試會擋:宣告 open 的州沒有鑰匙就失敗)
3. **掛圖(圖片)** —— 設計者 AI 生圖到手 → `assets/img/` → `data/media.json` → `DATA_VERSION` +1
4. **重跑平衡腳本** —— 事件庫從 86 長到 96、又多了地域加權,`node scripts/simulate.mjs 1000`
   與 `stresstest.mjs` 該再跑一次對照 M8 報告

## 三、等設計者拍板

| # | 事情 | 現況 |
|---|---|---|
| 1 | **低根骨命格會體力見底** | 體力上限約 1,090,打 10 場就見底,而現在一天 14 場;見底扣成功率 −50%。隔夜恢復綽綽有餘,是「單日連續打」的壓力。可視為設計如此,要調的旋鈕是每事件固定消耗 100 |
| 2 | **眼功仍偏向出門玩球** | 拳法練習補了空手管道(眼 30/分,與籃球同率),但桌球 60/分、高爾夫 50/分還是更快。可接受,也可再想 |
| 3 | **籃球軟功 0→10 是我自己加的** | 設計者只點名深蹲跳/交互蹲跳/足球/排球;籃球一併補是為了不讓不一致換個地方出現。要退掉是一行的事 |
| 4 | **`titles.defaultTitle` 仍是「無名之輩」** | 與評價矩陣同名。目前 `equippedTitle` 沒接到畫面所以看不到;接上時建議改「白身」 |

---

## 四、開發備忘

```bash
npm test            # 301 項
npm run serve       # http://localhost:5173(靜態頁,要伺服器,不能雙擊 html)
node scripts/simulate.mjs 1000    # 蒙地卡羅
node scripts/stresstest.mjs       # 疊加壓測
```

- **數值一律進 `data/*.json`,程式碼不寫死**——這是專案鐵律。
- **改過 `data/` 就要遞增 `src/ui/app.js` 的 `DATA_VERSION`**(目前 `p2-6`),否則 GitHub Pages 會拿到快取的舊資料檔。
- 決議要回寫 [一步一江湖_設計總綱_v2.md](一步一江湖_設計總綱_v2.md)(開工唯一依據)與 README 里程碑單。
