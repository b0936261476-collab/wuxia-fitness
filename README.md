# 一步一江湖 — 武林運動模擬器

玩家是武林中的小蝦米,透過真實運動累積六維武學能力(內功、輕功、硬功、軟功、眼功、耳功),透過日常走路步數推進江湖路並觸發劇情事件。用遊戲化維持運動動機。

完整設計見 [一步一江湖_設計總綱_v2.md](./一步一江湖_設計總綱_v2.md)(開工唯一依據);事件文案生產規範見 [事件庫生產規格書.md](./事件庫生產規格書.md),已過審內容見 [事件庫_正式庫.md](./事件庫_正式庫.md)。

## 兩條完全獨立的資源線

| 資源線 | 來源 | 用途 |
|---|---|---|
| 六維經驗值 | 主動記錄的 27 項運動 | 提升六項基本功等級,解鎖稱號 |
| 行動力(步數) | 手動輸入手機計步資料 | 每滿 1000 步觸發一次事件,推進江湖劇情 |

## 執行

純靜態網頁,無建置步驟。因為以 `fetch` 載入資料檔,需用本機伺服器開啟(不能直接雙擊 `index.html`):

```bash
npm run serve
```

然後開 <http://localhost:5173>。部署則直接丟 GitHub Pages 即可。

## 測試

```bash
npm test
```

以 Node 內建 test runner 驗證計算引擎:遞減分段(含規格書 §3 範例數字)、拆單防洗分、等級門檻、成功率夾限、支線流程、debuff 與物品等。

## 專案結構

```
data/
  exercises.json   ← 27 項運動的六維權重與遞減階梯(v2 §3)
  events.json      ← 事件庫 v2:《事件庫_正式庫》14 件全數入庫(秦大嫂線 1-1~1-4 + DA/CH/DU/FO)
  titles.json      ← 群俠錄 + 武道里程碑 + 均衡里程碑(v2 §8,等級門檻)
  items.json       ← 物品與 debuff 清單(M4 殘留,v2 事件已不發放 debuff)
  tags.json        ← 標籤字典 tagRegistry(v2 §0),M2 標籤引擎的地基
  triggers.json    ← 觸發器註冊表(v2 §9),PRIORITY_INTERRUPT/NARRATIVE_INJECT 種子資料
  quiz.json        ← 心理測驗創角題庫(15題)+ 26 條開局命格文案(v2 §1.2/§1.3)
  whispers.json    ← 天賦耳語文案池(⚠️ 僅 3 句種子,完整 23 句待從設計對話補入)
  npcs.json        ← 百強名冊(含 90 條被超越反應詞)+ 萬人總冊生成參數(v2 §9.5/§9.7/§9.9)
  reputation.json  ← 聲望階層門檻/評價矩陣(v2 §9.6)
src/
  engine/          ← 純邏輯計算引擎(無 DOM,Node 可直接測試)
    decay.js       ← 單日累積分階遞減
    exp.js         ← 等級門檻、里程碑
    tags.js        ← v2 §0.3 標籤判定引擎(唯一判定公式 + 疊加防爆三保險)
    events2.js     ← 事件引擎 v2:四段敘事、察覺/際遇判定(§9.11.1)、巢狀抉擇、
                       三段形態(§6.5)、fameVariants、勞務折銀(§9.11.4)
    triggers.js    ← v2 §9 觸發器優先序、NARRATIVE_INJECT 機率/冷卻判定
    check.js       ← ⚠️ 舊版判定公式,已被 tags.js 取代,保留供歷史對照
    game.js        ← 狀態與遊戲動作(練功、步數、事件、物品、榜單快照)
    storage.js     ← localStorage 存檔(含 v1→v2 事件遷移)
  ui/              ← 介面(vanilla JS + CSS)
tests/             ← 引擎測試(events2.test.mjs:察覺/樓梯/勞務/形態/自檢清單)
flag_registry.md   ← Flag 台帳(生產規格書第三節,與 events.json 同步維護)
index.html
```

> 完整設計依據見 [一步一江湖_設計總綱_v2.md](./一步一江湖_設計總綱_v2.md)(Claude Code 開工唯一依據文件)。

數值全部放在 `data/` 資料檔,調平衡不動程式碼。

## 開發進度(總綱 v2 §10.5 里程碑單)

- [x] **M0** 骨架與部署(GitHub Pages)
- [x] **M1** 資料 Schema + 種子內容
- [x] **M2** 標籤判定引擎 + 觸發器註冊表
- [x] **M3** 創角與成長鏈(心理測驗、悟性倍率、計時修煉、步數規則)
- [x] **M4** 三資源與狀態(血/內/體、DEBUFF 自動掛卸)
- [x] **M5** 事件迴圈 + 重生 + 防刷B
- [x] **M6** 榜單・稱號・聲望(群俠錄一條龍)
- [~] **M7** 內容灌裝與調性驗收 —— **進行中**:
  - [x] 正式庫 14 事件全數入庫(v2 schema:四段敘事/察覺判定/巢狀抉擇/三段形態/勞務折銀,引擎同步實裝)
  - [x] 26 條命格文案(quiz.json)
  - [x] 百強被超越反應詞 90 條(npcs.json)
  - [ ] 23 句天賦耳語(whispers.json 僅 3 句種子)——**待設計者提供定稿**
  - [ ] 6 種狀態敘事 ——**待設計者提供定稿**
  - [ ] 頒號敘事 6 段 ——**待設計者提供定稿**(總綱 §8.7 僅存輕功一段範本)
  - [ ] 序章 00–02 + 教學 03–06 事件 ——**待設計者提供定稿**
  - [ ] 驗收:設計者親自通玩 + 文案調性過稿(過稿清單見《事件庫生產規格書》第八節)
- [ ] **M8** 平衡驗證與收尾(蒙地卡羅 + 疊加壓測)

之後:**Phase 2** 群俠錄後端+江湖活化、**Phase 3** 內容擴充(見總綱 §10.2)。
